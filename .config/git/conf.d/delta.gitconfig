[pager]
    diff = delta
    show = delta
    log = delta
    blame = delta
    reflog = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    features = decorations
    line-numbers = true
    hyperlinks = true
    keep-plus-minus-markers = false

# vim: ft=gitconfig
